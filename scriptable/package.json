{
  "name": "scriptable-pollen",
  "private": true,
  "devDependencies": {
    "@types/node": "^22",
    "@types/scriptable-ios": "^1.7.9",
    "@typescript/native-preview": "^7.0.0-dev.20260511.1",
    "esbuild": "^0.25"
  },
  "peerDependencies": {
    "typescript": "^6"
  },
  "scripts": {
    "build": "esbuild ./src/pollen.ts --bundle --platform=neutral --target=esnext --outfile=pollen.js --global-name=Pollen",
    "typecheck": "tsgo"
  },
  "dependencies": {}
}
